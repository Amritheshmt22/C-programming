//POINTERS
//pointer is a variable that stores the memory address of another variable
//for declaring a pointer we use the * operator
     // data_type *pointer_name;
     //for example
     // int *ptr; //ptr is a pointer to an integer
     // float *ptr; //ptr is a pointer to a float
//we can also initialize a pointer at the time of declaration
//integer pointer can only point an integer variable and float pointer can only point a float variable
//we can also use the & operator to get the address of a variable
//for example
//