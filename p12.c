// program to display a number if it is negetive

#include<stdio.h>
int main() {
    int num;

    printf("Enter a number: ");
    scanf("%d", &num);

    if (num<0) {
        printf("-ve");
  }
  printf("+ve");
return 0;
}